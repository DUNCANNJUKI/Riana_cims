import { apiClient } from '@/integrations/apiClient';

export const setupOperators = async () => {
  console.log('setupOperators is deprecated for local API. Use backend migrations/scripts instead.');
};
